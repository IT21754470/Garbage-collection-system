//export const verifyEmployee = (req, res, next) => {
  //  if (!req.user.isEmployee) {
   //     return res.status(403).json({ success: false, message: 'Access denied. Only Employees are allowed.' });
   // }
   // next();
//};
